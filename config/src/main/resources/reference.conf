{
  rebar : {
    "accumulo" : {
      "useMock" : "true",
      "instance" : "accumulo",
      "zookeepers" : "localhost:2181",
      "user" : "root",
      "password" : "secret" 

      batchwriter : {
        latency : 5000000
        memory : 5
        threads : 4
        timeout : 3000
      }
    }
  
    "redis" : {
      "server" : "localhost:30001"
      "ingested-id-key" : "ingested_ids"
    }

    document-table-name : "documents"
    document-cf : "raw_doc"
    document-annotation-cf : "annotations"
    
    corpus-table-name : "available_corpora"
    corpus-prefix : "corpus_"

    stages-table-name : "available_stages"
    stages-prefix : "stage_"
    stages-object-cf : "stage_obj"

    stages-docs-cf : "documents"
    stages-docs-annotation-ids-cf : "annotated_ids"
  }
}